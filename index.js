const express = require('express');
const app = express();
const port = 5000;

async function run(){
    try{
        await client.connect();
    }
    finally{

    }

}

app.listen(port, () => {
  console.log(`Example app listening on port ${port}`)
})